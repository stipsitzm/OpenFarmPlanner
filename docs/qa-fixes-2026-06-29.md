# QA-Fixes – 29. Juni 2026

Behobene Issues aus dem QA-Testbericht `qa-report-2026-06-29.md`.

---

## BUG-M02 + BUG-L02 – RegisterPage: Passwort-Anzeigen-Buttons + autocomplete (Regression)

**Dateien:** `frontend/src/pages/auth/RegisterPage.tsx`

**Änderungen:**
- Beide Passwort-Toggle-Buttons verwenden jetzt `t('auth:register.showPassword')` / `t('auth:register.hidePassword')` statt hardcodierter englischer Strings – analog zu `LoginPage.tsx`
- Beide Passwort-`<TextField>`s tragen jetzt `slotProps.htmlInput.autoComplete: 'new-password'` für korrekte Passwort-Manager-Kompatibilität

**Tests:** `src/__tests__/RegisterPage.test.tsx`
- Neu: `password toggle buttons use translated German aria-labels (BUG-M02 regression guard)`
- Neu: `password fields carry autocomplete="new-password" (BUG-L02 regression guard)`

---

## BUG-L01 – CommandProvider: Tipp-Banner ohne i18n

**Dateien:**
- `frontend/src/commands/CommandProvider.tsx`
- `frontend/src/i18n/locales/de/navigation.json`

**Änderungen:**
- Neuer Übersetzungsschlüssel `commandPalette.shortcutHint` in `navigation.json` hinzugefügt
- Banner-Text `'💡 Tipp: Drücke Alt+K für die Command Palette.'` durch `t('commandPalette.shortcutHint')` ersetzt

---

## BUG-M01 – Ungültige Routen zeigen leere Seite

**Dateien:** `frontend/src/App.tsx`

**Änderungen:**
- Catch-all-Route `{ path: '*', element: <Navigate to="/app/dashboard" replace /> }` am Ende der RootLayout-Kinder eingefügt → unbekannte `/app/*`-Pfade leiten zum Dashboard weiter
- Catch-all-Route `{ path: '*', element: <Navigate to="/" replace /> }` auf Top-Level eingefügt → unbekannte öffentliche Pfade leiten zur Homepage weiter

**Tests:** `src/__tests__/App.test.tsx`
- Neu: `redirects authenticated users from unknown /app/* routes to dashboard (BUG-M01 regression guard)`
- Neu: `redirects unknown top-level routes to home`

---

## UX-01 / INC-01 – Projekteinstellungen im Desktop-Mehr-Menü

**Dateien:** `frontend/src/App.tsx`

**Änderungen:**
- `desktopMenuItems` in `GlobalMenu` um Eintrag `desktop-project-settings` ergänzt (mit `SettingsOutlinedIcon` + `t('project.settings')`)
- Visueller Trenner (`<Divider>`) zwischen Projektaktionen (Projekteinstellungen, Versionsverlauf) und App-Aktionen (Kontoeinstellungen, Tastenkürzel, …) eingefügt
- Projekteinstellungen ist jetzt auf Desktop direkt im Mehr-Menü erreichbar, nicht mehr nur über den Projektwechsler-Dropdown

**Tests:** `src/__tests__/App.test.tsx`
- Bestehender Test `shows account settings in three-dot menu without project settings` auf `shows account settings and project settings in three-dot menu on desktop` aktualisiert und Erwartung umgekehrt (Projekteinstellungen wird jetzt erwartet)

---

## UX-02 – Kein Hinweis für Nicht-Admins auf Projekteinstellungen-Seite

**Dateien:**
- `frontend/src/pages/ProjectSettingsPage.tsx`
- `frontend/src/i18n/locales/de/projectInvitations.json`

**Änderungen:**
- Neuer Übersetzungsschlüssel `memberManagementNoAccess`: `"Du siehst diese Seite als Mitglied. Für die Mitgliederverwaltung benötigst du Admin-Rechte."`
- Info-`Alert` direkt oberhalb der Einladesektion eingefügt, der nur für `!canManageMembers` sichtbar ist – Nicht-Admins sehen sofort einen klärenden Hinweis, bevor sie auf die deaktivierten Formularfelder stoßen

---

---

## K-01 – Kein maxLength auf Kulturname und Sorte

**Dateien:** `frontend/src/cultures/sections/BasicInfoSection.tsx`

**Änderungen:**
- `slotProps={{ htmlInput: { maxLength: 200 } }}` auf Name- und Sorte-TextField hinzugefügt – entspricht dem Backend-Limit (`max_length=200` in `Culture.name`)

**Tests:** `src/__tests__/cultureSections.test.tsx`
- Neu: `name and variety fields enforce maxLength=200 (K-01 regression guard)`

---

## MOB-02 – Mobiler Add-Button zu kleines Touch-Target (32×36 px)

**Dateien:** `frontend/src/App.tsx`

**Änderungen:**
- `minWidth: isPhone ? 36 : 'auto'` auf `minWidth: isPhone ? 44 : 'auto'` erhöht
- `minHeight: isPhone ? 44 : 'auto'` hinzugefügt
- Resultat: primärer Add-Button auf Mobilgeräten erfüllt jetzt WCAG 2.5.5 (44×44 px Touch-Target)

---

## Teststatus nach allen Fixes

```
Test Files: 94 passed (94)
Tests:      838 passed (838)
```
